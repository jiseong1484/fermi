spring.application.name=fermi-signaling-server

server.port=${PORT:8080}
server.address=0.0.0.0


# ???(?? ???) ??? URL: python http server 8000??
app.frontendBaseUrl=${PUBLIC_BASE_URL:http://localhost:8000}

#?? ?? ??(?)
app.sessionTtlMinutes=${SESSION_TTL_MINUTES:30}

# --- Database Configuration ---
spring.datasource.url=jdbc:mariadb://localhost:3306/fermi
spring.datasource.username=fermi
spring.datasource.password=kim1484
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- JPA Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect